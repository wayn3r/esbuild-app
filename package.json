{
  "name": "esbuild-starter-app",
  "version": "1.0.0",
  "description": "React App using esbuild",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --inject:./config/runtime-jsx.js --outdir=build --loader:.js=jsx --minify --loader:.png=file --loader:.jpg=file --asset-names=static/[name].[hash]"
  },
  "keywords": [],
  "author": "wayn3r",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.21",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
